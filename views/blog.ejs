<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog app</title>

    <%- include('./partials/head') %>

    <style>
        
        body {
            background-color: #121212;
            color: #ffffff;
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
        }

        .container img {
            max-width: 100%;
            height: auto;
            border-radius: 5px;
        }

        pre {
            background-color: #1e1e1e;
            color: #ffffff;
            padding: 15px;
            border-radius: 5px;
            overflow-x: auto;
            white-space: pre-wrap; /* Allow line breaks */
            word-wrap: break-word; /* Break long words */
        }

        .card {
            background-color: #1e1e1e;
            border: none;
            border-radius: 10px;
            margin-bottom: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .card-body {
            background-color: #2c2c2c;
            border-radius: 10px;
            padding: 30px;
        }

        h1, h5 {
            color: #ffffff;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }

        .profile {
            display: flex;
            align-items: center;
            margin-top: 20px;
            background-color: #1e1e1e;
            padding: 10px;
            border-radius: 10px;
        }

        .profile img {
            border-radius: 50%;
            margin-right: 15px;
        }

        .profile h5 {
            margin: 0;
        }

        .content-section {
            margin-bottom: 30px;
        }
        
        .content-section p {
            margin-bottom: 1.5rem;
        }
    </style>
</head>
<body>
    <%- include('./partials/script') %>
    
    <%- include('./partials/nav') %>
    <div class="container mt-3">
      <h1> <%= Blog.title %></h1>
      <img src="<%= Blog.imageurl %>">
      <pre><%= Blog.body %></pre>


    </div>
    <div class="container mt-3">
        Written by <br>
        <img src="<%= Blog.createdby.profileImageURL %> " style="height: 50px;">
        <%= Blog.createdby.name %>
    </div>
    <div class="container mt-3">
        <h1 class="mb-4">Comments(<%= Comment.length %>)</h1>
    </div>
    
  <% if (locals.user) { %>
    <div class="container mt-3">
        
        <form action="/blogs/comments/<%= Blog._id %>" method="POST" > 
          <div class="mb-3">
            <input type="text" class="form-control" name="content" placeholder="Enter your Comment">
          </div>
          <button type="submit" class="btn btn-primary">Submit</button>
        </form>
      </div>
      <% } %>
      
<% Comment.forEach(comment => { %>
    

      <div class="container mt-3">
        <img src="<%= comment.createdby.profileImageURL %> " style="height: 50px;">
       <%= comment.createdby.name %><br>
        <%= comment.content %>
    </div>
    <% });  %>

    
   

    
    
</body>
</html>